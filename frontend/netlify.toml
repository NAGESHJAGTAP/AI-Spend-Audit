# Public share pages are served by this SPA.
# Route /share/:shareId -> index.html so React can fetch /api/audit/share/:shareId
[[redirects]]
  from = "/share/*"
  to = "/index.html"
  status = 200
  force = true


